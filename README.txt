#CS221 project

1) to update data, execute "build_dataset.py", settings are controlled via "\data\data_params\data_params.json"
2) to run the model, execute "model_main.py", results will be stored in \results folder
2.1) flags "decay" and "dist_check" are used to enable modification 1 and 2 respectively
